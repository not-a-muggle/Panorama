apiVersion: v1
kind: Service
metadata:
  name: image-service
spec:
 type: ClusterIP
 ports:
  - targetPort: 50054
    port: 50054
    nodePort: 30900 
 selector:
  app: image-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-service
spec:
  selector:
    matchLabels:
      app: image-service
  replicas: 1
  template:
    metadata:
      labels:
        app: image-service
        tier: backend

    spec:
      hostNetwork: true
      nodeSelector:
        worker : "2"
      containers:
      - name: image-service
        imagePullPolicy: Always
        image: panoramaiubads/image-service
        ports:
        - containerPort: 50054
      restartPolicy: Always