version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  build:

    docker:
      - image: circleci/node:buster
    steps:
      - setup_remote_docker
      - checkout
      - run: cd ./session-service/ && npm install
      - run: cd ./session-service/ && npm run test
      - run: cd ./session-service/ && npm run compile
      - run: cd ./session-service/ && npm run buildimage

workflows:
  build:
    jobs:
      - build:
          filters:
            branches:
              only:
                - session-service-ci
