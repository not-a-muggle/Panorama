syntax = "proto3";
package img;


service ImageService {
    rpc getImage(ImageLocation) returns (Image);
    rpc getImages(ImageLocation) returns (Image);
}

message ImageLocation {
    string userId = 1;
    string imageId = 2;
}

message Image {
    string imageData = 1;
}
