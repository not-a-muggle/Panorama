apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service-deploy
  labels:
    app: gateway-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
        updateTimestamp: "20210410205518106"
    spec:
      containers:
      - name: gateway-service
        image: panoramaiubads/gateway-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5003
        resources:
          requests:
            cpu: 250m
          limits:
            cpu: 500m
            memory: 2Gi
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: gateway-service
  name: gateway-svc
spec:
  selector:
    app: gateway-service
  ports:
    - nodePort: 302000
      port: 3000
      targetPort: 3000
  type: LoadBalancer